# Explainable AI
Part of the Advanced Deep Learning Module (MSE Data Science, HS 25/26)


### Stripout Jupyter Notebook Execution
automatically strip outputs for commits
`nbstripout --install --attributes .gitattributes`


### uv set-up
install venv:
`uv sync`

done:
`uv init`
`uv add --from-requirements requirements.txt` 
--> pyproject.toml + .python-version were generated